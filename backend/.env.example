# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/focusaint

# JWT Secret
JWT_SECRET=focusaint_secret_key_change_in_production

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Email Configuration
# Choose email service: 'gmail' or leave empty for custom SMTP
EMAIL_SERVICE=gmail

# Email credentials
# For Gmail: Use your Gmail address and App Password
# Generate App Password at: https://myaccount.google.com/apppasswords
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here

# Custom SMTP Configuration (if not using Gmail)
# Uncomment and configure these if you're using a custom SMTP service
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Example for other email services:
# SendGrid: SMTP_HOST=smtp.sendgrid.net, SMTP_PORT=587
# Mailgun: SMTP_HOST=smtp.mailgun.org, SMTP_PORT=587
# AWS SES: SMTP_HOST=email-smtp.us-east-1.amazonaws.com, SMTP_PORT=587
